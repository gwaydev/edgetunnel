{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:pages": "npm run build && node scripts/prepare-pages.mjs",
    "test": "node --test test/*.test.mjs",
    "deploy:dry": "npm run build && wrangler deploy --dry-run --outdir tmp-wrangler-dry-run --config wrangler.worker.toml"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "wrangler": "4.129.0"
  }
}

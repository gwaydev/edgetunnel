{
  "scripts": {
    "build": "node scripts/build.mjs",
    "syntax": "node --check _worker.js && node --check dist/worker.js",
    "pretest": "npm run build",
    "test": "node --test test/*.test.mjs",
    "deploy:dry": "npm run build && wrangler deploy --dry-run --outdir tmp-wrangler-dry-run"
  },
  "devDependencies": {
    "esbuild": "0.25.8",
    "wrangler": "4.61.0"
  }
}

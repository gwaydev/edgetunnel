name = "edgt1"
pages_build_output_dir = "dist-pages"
compatibility_date = "2025-11-04"

# Pages production bindings are configured in the Cloudflare dashboard.

# Git deployments must run npm run build:pages and publish dist-pages.

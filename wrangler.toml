name = "v20251104"
main = "_worker.js"
compatibility_date = "2025-11-04"
keep_vars = true

# Production Xboard authorization uses this exact binding name.
# Create/select the KV namespace first, then uncomment and replace the placeholder.
# Never commit a real namespace ID to a public repository.
# [[kv_namespaces]]
# binding = "XBOARD_KV"
# id = "REPLACE_WITH_KV_NAMESPACE_ID"